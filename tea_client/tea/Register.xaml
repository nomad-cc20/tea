<Page
    x:Class="tea.Register"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tea"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
            <TextBlock x:Name="infoTb" TextWrapping="WrapWholeWords" HorizontalAlignment="Center" />
            <TextBox x:Name="usernameTb" PlaceholderText="Username" />
            <PasswordBox x:Name="pwdTb" PlaceholderText="Password" />
            <PasswordBox x:Name="pwdTb2" PlaceholderText="Password" />
            <TextBox x:Name="nameTb" PlaceholderText="Display name" />
            <TextBox x:Name="phoneTb" PlaceholderText="Phone number" />
            <Button Content="Register" Width="400" Click="Button_Click"/>
        </StackPanel>
        <Button HorizontalAlignment="Center" VerticalAlignment="Bottom" Content="Back" Width="400" Click="Button_Click_1"/>
    </Grid>
</Page>
