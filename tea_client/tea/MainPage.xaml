<Page
    x:Class="tea.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:tea"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="40" Al/>
        </Grid.RowDefinitions>
        <Button Content="Offer" VerticalAlignment="Center" HorizontalAlignment="Center" Grid.Row="0"/>
        <Button Content="Browse" VerticalAlignment="Center" HorizontalAlignment="Center" Width="100" Grid.Row="1"/>

    </Grid>-->
    <ListView x:Name="offers" HorizontalAlignment="Stretch">
        <ListViewItem Content="Home" />
        <ListView.ItemTemplate>
            <DataTemplate>
                <Grid>

                    <TextBlock Text="{Binding Name}"></TextBlock>

                    <TextBlock Text="{Binding Description}"></TextBlock>

                    <TextBlock Text="{Binding User}"></TextBlock>

                </Grid>
            </DataTemplate>
        </ListView.ItemTemplate>
        <ListView.Footer>
            <Button Content="New" HorizontalAlignment="Stretch" />
        </ListView.Footer>
    </ListView>
</Page>
